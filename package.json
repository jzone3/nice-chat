{
  "name": "nice-chat",
  "version": "1.0.0",
  "description": "One big chat room where only nice messages get through. Powered by Jev.",
  "main": "server.js",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check jev.js && node --check store.js && node --check public/app.js",
    "test": "node --test test/*.test.js"
  },
  "license": "MIT"
}
